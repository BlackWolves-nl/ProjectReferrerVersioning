<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Dark Theme Styles -->
    <Color x:Key="PrimaryBackgroundColor">#222</Color>
    <Color x:Key="SecondaryBackgroundColor">#2A2A2A</Color>
    <Color x:Key="PrimaryForegroundColor">#EEE</Color>
    <Color x:Key="AccentColor">#0078D4</Color>
    <Color x:Key="BorderColor">#555</Color>
    <Color x:Key="RootNodeBorderColor">#00BFFF</Color> <!-- DeepSkyBlue -->
    <Color x:Key="HoverBorderColor">#FFA500</Color> <!-- Orange -->
    <Color x:Key="BadgeBackgroundColor">#E53935</Color> <!-- Red -->
    <Color x:Key="BadgeForegroundColor">#FFFFFF</Color> <!-- White -->
    <Color x:Key="DataGridRowHoverColor">#808080</Color> <!-- Much more visible hover color -->
    <Color x:Key="DataGridRowSelectedColor">#666666</Color> <!-- Selected row color -->
    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="{StaticResource PrimaryBackgroundColor}"/>
    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="{StaticResource SecondaryBackgroundColor}"/>
    <SolidColorBrush x:Key="PrimaryForegroundBrush" Color="{StaticResource PrimaryForegroundColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
    <SolidColorBrush x:Key="BorderBrushColor" Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="ExcludedProjectForegroundBrush" Color="#A0A0A0"/>
    <SolidColorBrush x:Key="ProjectStatusInitialBrush" Color="#D3D3D3"/>
    <SolidColorBrush x:Key="ProjectStatusModifiedBrush" Color="#FF6B6B"/>
    <SolidColorBrush x:Key="ProjectStatusNuGetOrProjectReferenceChangesBrush" Color="#FFA500"/>
    <SolidColorBrush x:Key="ProjectStatusNuGetOrProjectReferenceAndVersionChangesBrush" Color="#483D8B"/>
    <SolidColorBrush x:Key="ProjectStatusIsVersionChangeOnlyBrush" Color="#4682B4"/>
    <SolidColorBrush x:Key="ProjectStatusCleanBrush" Color="#228B22"/>
    <SolidColorBrush x:Key="RootNodeBorderBrush" Color="{StaticResource RootNodeBorderColor}"/>
    <SolidColorBrush x:Key="HoverBorderBrush" Color="{StaticResource HoverBorderColor}"/>
    <SolidColorBrush x:Key="BadgeBackgroundBrush" Color="{StaticResource BadgeBackgroundColor}"/>
    <SolidColorBrush x:Key="BadgeForegroundBrush" Color="{StaticResource BadgeForegroundColor}"/>
    <SolidColorBrush x:Key="DataGridRowHoverBrush" Color="{StaticResource DataGridRowHoverColor}"/>
    <SolidColorBrush x:Key="DataGridRowSelectedBrush" Color="{StaticResource DataGridRowSelectedColor}"/>
    <Style TargetType="Button" x:Key="ThemedButtonStyle">
        <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="2"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#444"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="TextBox" x:Key="ThemedTextBoxStyle">
        <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="CaretBrush" Value="{StaticResource PrimaryForegroundBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
    </Style>
    <Style TargetType="Label" x:Key="ThemedLabelStyle">
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
    </Style>
    <Style TargetType="TextBlock" x:Key="ThemedTextBlockStyle">
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
    </Style>
    <Style TargetType="TabItem" x:Key="ThemedTabItemStyle">
        <Setter Property="Background" Value="{StaticResource SecondaryBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}">
                        <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#888"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
